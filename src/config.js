// Base path the site is served from on GitHub Pages.
// Must stay in sync with `basePath` in next.config.js.
// next/image does not prepend basePath to image src when images are
// `unoptimized`, so asset paths must include it explicitly.
export const basePath = "/chesspawngame";
